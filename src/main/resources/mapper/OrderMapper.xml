<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="yhy.project.mapper.OrderMapper">

    <!-- 添加订单 -->
    <insert id="insertOrder" parameterType="Order" keyColumn="id" keyProperty="id" useGeneratedKeys="true">
        insert into `order`(order_number,total,amount,status,paytype,name,phone,address,systime,user_id)
        values(#{orderNumber},#{total},#{amount},#{status},#{paytype},#{name},#{phone},#{address},#{systime},#{userId})
    </insert>

    <!-- 根据用户id查询订单 -->
    <select id="selectOrdersByUserId" parameterType="Integer" resultType="OrderVo">
        select * from `order` where user_id = #{userId}
    </select>
    <!--获取所有订单,也可以根据状态进行查询,-->
    <select id="getAllOrders" parameterType="Integer" resultType="OrderVo">
        select * from `order`
        <where>
            <if test="orderNumber != null">
                and order_number like concat('%',#{orderNumber},'%')
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="paytype != null">
                and paytype = #{paytype}
            </if>
        </where>
    </select>
    <!--修改订单状态-->
    <update id="updateOrderStatus">
        update `order` set status = #{status} where id = #{id}
    </update>

</mapper>